{
  "name": "cse-security-scanner",
  "version": "1.0.0",
  "description": "Un escáner de seguridad de dominios que se ejecuta en Cloudflare Workers.",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute security-scans --file=./initial.sql"
  },
  "dependencies": {
    "itty-router": "^4.0.27"
  },
  "devDependencies": {
    "wrangler": "^3.62.0"
  }
}
